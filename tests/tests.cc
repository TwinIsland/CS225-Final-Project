#include "catch.hpp"
#include "solution.hpp"

#include "include.hpp"
#include "graph.h"

using std::string;
using std::set;
using std::vector;
using std::pair;
using std::list;


TEST_CASE("test1") {
    REQUIRE(true == true);
}

TEST_CASE("readfile and map construct") {
    vector<inputVertex> i_node;
    vector<Edge> i_edge;
    
    
}